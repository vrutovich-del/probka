# dependencies & build output
node_modules/
dist/
dev-dist/
*.tsbuildinfo

# wrangler's local D1/R2 state, its local secrets, and the binding types it generates from wrangler.toml
/server/.wrangler/
/server/.dev.vars
/server/worker-configuration.d.ts

# local env & editor noise
.env
.env.*
*.local
.claude/settings.local.json
.DS_Store
Thumbs.db

# the handoff zip; its contents live in design/
/*.zip

# background-removal model + runtime, ≈170 MB — `npm run cutout-model` mirrors it from the IMG.LY CDN
/public/cutout/
